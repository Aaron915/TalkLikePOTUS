import CurrentGameDetail from '../currentGame/CurrentGameDetail/CurrentGameDetail'

// Object that is meant to be combined with parameters of other navigators
export const currentGameNav = {
    CurrentGameDetail: {
        screen: CurrentGameDetail
    }
}