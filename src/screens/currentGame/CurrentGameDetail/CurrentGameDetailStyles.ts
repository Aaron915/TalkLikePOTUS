import { lightGreyColor } from './../../../constants/colors'
import { StyleSheet } from 'react-native'

export const currentGameDetailStyles = StyleSheet.create({
    main: {
        flex: 1,
        backgroundColor: lightGreyColor
    }
})