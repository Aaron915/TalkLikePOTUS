import { createAction } from 'redux-actions'

export const hasSeenNotificationPrompt =
    createAction('HAS_SEEN_NOTIFICATION_PROMPT')

export const hasSeenTutorialPrompt = createAction('HAS_SEEN_TUTORIAL_PROMPT')